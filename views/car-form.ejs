<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= car._id ? 'Edit' : 'Add' %> Car</title>
</head>
<body>
  <h1><%= car._id ? 'Edit' : 'Add' %> Car</h1>
  <form action="/cars<%= car._id ? '/' + car._id : '' %>" method="POST">
    <label for="name">Name:</label>
    <input type="text" id="name" name="name" value="<%= car.name %>" required>

    <label for="image">Image URL:</label>
    <input type="text" id="image" name="image" value="<%= car.image %>" required>

    <label for="price">Price:</label>
    <input type="number" id="price" name="price" value="<%= car.price %>" required>

    <label for="currency">Currency:</label>
    <select id="currency" name="currency" required>
      <option value="USD" <%= car.currency === 'USD' ? 'selected' : '' %>>USD - US Dollar</option>
    </select>

    <label for="city">City:</label>
    <input type="text" id="city" name="city" value="<%= car.city %>" required>

    <button type="submit"><%= car._id ? 'Update' : 'Add' %> Car</button>
  </form>
</body>
</html>
